# CLAUDE.md — see AGENTS.md

MapExport keeps its AI working notes in one tool-agnostic file so Claude Code
and Codex (and whatever comes next) share a single source of truth. The full
contract — build, deploy, testing, the mandatory changelog rule, the
cost-aware delegation tiers, and the conventions worth preserving — lives in
`AGENTS.md`, imported below. **Edit `AGENTS.md`, not this file.**

Claude Code auto-loads this `CLAUDE.md` but not `AGENTS.md`, so this stub
exists only to pull the universal file in. Codex reads `AGENTS.md` directly.

@AGENTS.md
