# Active maintenance checkpoint

- **Updated:** 2026-07-14
- **Roadmap:** `plans/2026-07-14_codebase-maintenance-priorities.md`
- **Sprint:** Sprint 1 — Betrouwbaar exportcontract (`IN PROGRESS` — aan de eindpoort, live-sweep open)
- **Unit:** ME-03 — v2-dekkingsbelofte herstellen zonder wegen (`DONE`, code+offline; live v2-sweep `WAITING_FOR_USER`)
- **Owner/route:** O voerde ME-03 rechtstreeks uit; geen agentdelegatie.
- **Completed checkpoint:** ME-03 code+offline afgerond. De vroegtijdige lege return in `computeFacesAsync` (bij nul cutterLines) is verwijderd, zodat een frame zonder block-cutting roads via de normale Clipper-route één full-frame face krijgt en de coverage-belofte (ENGINE-V2.md §1) nakomt; `minArea` en de countryside-uitzondering zijn ongemoeid. Nieuwe offline test `tests/v2-cutterless-coverage.mjs` dekt geen-wegen/alleen-paden/alleen-tunnels (prepareFaceData → 0 cutterLines) plus een gestubde-worker bewijs dat computeFacesAsync niet meer kortsluit; bewezen dat de test faalt als de return terugkomt. Test toegevoegd aan `tests/smoke.sh`, `tests/README.md`, `AGENTS.md` en de roadmap-baseline. Roadmapcheckbox ME-03 afgevinkt, changelog-entry toegevoegd.
- **Next action:** Sprint 1 eindpoort afronden — de enige resterende, verplichte controle is de handmatige v1/v2-exportcontrole na enginewijziging (ENGINE-V2.md §9: de zeven-area v2-sweep `node tests/real-export.mjs <city> --engine=v2` met nul allowances, plus visuele Tilburg-goedkeuring). Die vereist `lamp start` + browser en menselijke sign-off; niet in deze sessie gedraaid. Draai daarna, als groen, Sprint 1 op `COMPLETE` met het sprint-reviewblok en start Sprint 2 (ME-04).
- **Changed for current unit:** `engine-v2.js`, `tests/v2-cutterless-coverage.mjs` (nieuw), `tests/smoke.sh`, `tests/README.md`, `AGENTS.md`, `CHANGELOG.md`, roadmap en dit checkpoint. Bestaande untracked exports en het lokale Overpass-plan bleven ongemoeid.
- **Latest checks:** `OFFLINE_ONLY=1 bash tests/smoke.sh` groen (incl. de nieuwe test), `node tests/sea-sign.mjs` en `node tests/hamlet-grounding.mjs` groen, `node --check script.js|engine-v2.js` en `php -l cache.php` groen, `git diff --check` schoon. Bewezen dat `tests/v2-cutterless-coverage.mjs` faalt met de return terug (exit 1) en slaagt zonder. De zeven-area v2-sweep / render-coverage-ink-check is NIET gedraaid (browser + menselijke goedkeuring vereist).
- **Decisions/blockers:** De worker verwerkte lege cutterLines al (voidClean leeg → face = frame); de fix is puur het weghalen van de kortsluiting. Blocker voor sprintafsluiting: de verplichte live v2-sweep + visuele sign-off staat bij Coen (`WAITING_FOR_USER`).
